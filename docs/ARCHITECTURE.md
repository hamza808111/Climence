# Architecture

> 📌 **Moved.** The authoritative architecture document now lives at the repo root: [../ARCHITECTURE.md](../ARCHITECTURE.md).
>
> The new doc reflects the current flat layout (`backend/`, `frontend/`, `simulator/`, `shared/`), includes the full system diagram, layer responsibility table, and two end-to-end data-flow walkthroughs (one telemetry tick + one user action with RBAC).
>
> This stub is kept here so external links to `docs/ARCHITECTURE.md` still resolve.
